#include <stdio.h>
/**
 * main - to print the number of arrays
 * @argc: the argument counter
 * @argv: the argument vector
 * Return: to return 0
 */
int main(int argc, char *argv[])
{
	(void) argv;

	printf("%d\n", argc - 1);
	return (0);
}
