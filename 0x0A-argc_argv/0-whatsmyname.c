#include <stdio.h>
/**
 * main - print the name
 * @argc: the argument counter
 * @argv: the argumnent vector
 * Return: to return 0
 */
int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
